# 次セッションへの引き継ぎ（2026-08-22）

## 状態：本番反映済み（2026-08-22）

GAS・フロントとも反映完了。`origin/main` は `15273e9`。

**本番で検証済み**（読み取りのみ、データには触れていない）：

| 試したこと | 応答 |
|---|---|
| トークン無しで `getAllMonths` | `AUTH` — 拒否 |
| デタラメなトークン | `AUTH` — 拒否 |
| **旧 API_SECRET** | `STALE_CLIENT` — 拒否 |
| GET でアクセス | `GETは利用できません` |

配信側も確認済み：`js/config.js` から `API_SECRET` 消滅、`?v=20260822a` の新コードが配信中。
**公開リポジトリに載っていた旧合言葉は、もう何の権限も持たない。**

※書き込み(`saveEntry`)の拒否は本番データを汚す危険があるため試していない。
トークン検証は読み書き共通の経路（`doPost` の入口）なので、読み取り側の結果で足りる。

### 残っている作業

- [ ] 家族にパスワードを伝える（各自1回ログインし直し）
- [ ] `exportMonthlyReport` を手動実行し、Drive の `2026年07月.json` が20KB前後のままか確認
- [ ] （急がない）`removeLegacySecret()` で旧 API_SECRET プロパティを片付ける

### GAS側の作業をする場所

**turu728@gmail.com（母）のアカウント。**家計簿スプレッドシートもApps Scriptもそこにあり、
ユーザー(hyoboyh@gmail.com)のDriveには共有されていない（共有されているのは出力先フォルダだけ）。

### 再デプロイの手順（次回のために）

1. `gas/Code.gs` を貼り付けて保存
2. `benchmarkHash()` で計測 → 必要なら `PWD_ITERATIONS` を調整
   （**必ず `setupPassword` より先に。** 回数は setupPassword 実行時に記録へ焼き付くため）
3. `setupPassword('...')` は引数を取るので関数選択欄から直接実行できない。一時的に
   `function tmp() { setupPassword('...'); }` を書いて `tmp` を実行し、**実行後その行を消して保存**
4. 「デプロイを管理 → 編集 → 新しいバージョン」（保存だけ／「新しいデプロイ」ではURLが変わる）
5. フロントを push（`?v=` を上げること）。**4と5の間はアプリが止まる**ので続けてやる

---

## このセッションでやったこと：パスワード認証をサーバー側で本物にした

### なぜやったか

`js/config.js` は GitHub Pages 配信の都合でコミットする必要があり、公開リポジトリに
`GAS_URL` と `API_SECRET` の実値が載っていた。一方フロントのパスワードは端末内の
localStorage のハッシュ比較だけで、サーバーは一切見ていなかった。
つまり **リポジトリを見た第三者が誰でも家計データを読み書きできる状態**だった。

### どう直したか

鍵を「フロントに書けないもの」＝家族が覚えるパスワードに一本化した。

- **パスワードはGASのスクリプトプロパティにソルト付き反復SHA-256で保存**。平文は保存しない
- **ログイン時に `action:'login'` でサーバー照合** → 成功した端末にだけトークンを発行
- **以降の全リクエストはトークンで認証**。`API_SECRET` は完全に廃止し `config.js` から削除
- **`config.js` に残るのは `GAS_URL` だけ**。URLを知られてもパスワードが無ければ弾かれるので、
  **リポジトリは公開のままで構わなくなった**

### 主なパラメータ（`gas/Code.gs` 冒頭の認証セクション）

| 定数 | 値 | 意味 |
|---|---|---|
| `PWD_ITERATIONS` | 2000 | ハッシュの反復回数。本番実測(2026-08-22)：12000回=11063ms(冷)、2000回=823ms(温)。冷えた状態なら約1.8秒 |
| `TOKEN_TTL_DAYS` | 90 | トークンの有効期間 |
| `TOKEN_RENEW_DAYS` | 30 | 残りがこれを切ったら黙って延長（ログインし直しを避ける） |
| `MAX_SESSIONS` | 20 | 保持する端末数。あふれたら古い順に落ちる |
| `LOGIN_FAIL_MAX` | 10 | 15分あたりの失敗上限。超えるとロック |

### エディタから使う関数

- `setupPassword('...')` … パスワード設定／リセット。全端末ログアウト
- `removeLegacySecret()` … 旧API_SECRETプロパティの片付け。全部の動作確認が済んでから。急がなくてよい
- `resetLoginLock()` … ログイン失敗ロックの手動解除（家族が締め出された時の非常口）
- `benchmarkHash()` … ハッシュ計算時間の実測

### 移行の扱い（即時切り替えを選択）

旧 `secret` は一切受け付けない。ただし旧フロントが `secret` だけを送ってきた場合は
`code: 'STALE_CLIENT'` を返し、フロント側がログイン画面へ引き戻す。
**未同期の記録（`kakeibo_pending`）はログアウト時もトークン失効時も消さない**ので、
古い端末が残っていても入力は失われない。

### 検証したこと

- **GAS側**：`Utilities`/`PropertiesService`/`CacheService` をシムして認証ロジックを実際に実行。
  44項目すべて通過（scratchpadの `auth_sim.js`。本番シートには触れていない）
  - setupPassword前は全拒否／平文が保存されないこと／ソルトとハッシュの形
  - 誤パスワード拒否、トークン発行、有効期限90日
  - デタラメなトークン・トークン無し・旧secretのみ の拒否
  - 失敗10回でロック → `resetLoginLock()` と15分経過の両方で解除
  - 89日後も有効・残り30日で自動延長・91日後は失効
  - ログアウトで即失効
  - パスワード変更：旧パスワード検証、他端末のトークン失効、変更端末は継続
  - セッション数上限で古い端末から落ちること
  - ハッシュの決定性・ソルト依存・反復数依存・区切り文字による連結の曖昧さ回避
- **フロント側**：ローカル（port 3456）で `fetch` をモックに差し替えて実測。本番GASへは1回も送っていない
  - 誤パスワード → エラー表示・トークン未保存・ボタン復帰
  - 正パスワード → トークン保存・入力欄クリア・アプリ画面へ・以降の送信に token が付く
  - トークン失効／STALE_CLIENT → ログイン画面へ引き戻し・トークン消去・**未同期キューは保持**
  - パスワード変更：短すぎる新パスワードは通信前に弾く／旧パスワード誤りでログイン画面に飛ばない／
    成功で新トークンに差し替え
  - ログアウト：未同期件数を確認ダイアログで警告・サーバーへ logout 送信・月キャッシュ消去・
    **未同期キューは保持**
  - ログイン → ダッシュボード描画まで JSエラーなし

---

## 月次JSON出力（家計だより用）— 決着済み、ただし本番へ反映待ち

`exportMonthlyReport`（時間主導トリガー、毎月16日）は **`getMonthData` を直接呼んで
`DriveApp` で書き出すだけ**だった。`UrlFetchApp` も `API_SECRET` も使っておらず、
`doPost` を通らないため、**今回の認証変更とは無関係**だった。

### ただし一度失った

この関数は本番の `Code.gs` にだけ存在し、リポジトリには入っていなかった。
「`gas/Code.gs` を貼り付けて保存」の手順で上書きされ、消えた。
ユーザーの控えから復元し、今回リポジトリへ入れた。

**教訓：本番の Code.gs を貼り替える前に、リポジトリに無いコードが本番側に
無いか必ず確認すること。**リポジトリが本番の全体を持っている保証は無かった。

### 反映（まだ）

- [ ] `gas/Code.gs` を Apps Script へ貼り付けて**保存**
  - **再デプロイは不要。**時間主導トリガーは保存済みコードを実行する（デプロイ版ではない）。
    ウェブアプリ側の認証コードは変えていないので、デプロイ版は今のままでよい
  - 初回実行時に **Drive の承認画面**が出る可能性がある（今の保存版に `DriveApp` が
    無いため権限が外れている）。出たら許可する
- [ ] `exportMonthlyReport` を手動実行して確認

### 確認時の注意：ファイルサイズは前より大きくなる

`getMonthData` は 2026-08-21 に取引ログの `items` を返すようになった。
`exportMonthlyReport` は `getMonthData` の戻りをそのまま書くので、**出力にも
`items` が入る**。8/16に出た `2026年07月.json` は23,561バイトだったが、
今回はこれより大きくなるのが正常。小さくなっていたら異常。

`items` を落として以前と同じ形にしたい場合は、`exportMonthlyReport` 側で
削ってから `JSON.stringify` する。下流（家計だより）は追加フィールドがあっても
壊れないので、急ぎではない。

### 既知の挙動（バグではないが把握しておく）

対象月を「当月1日」から決めるため、**月の前半（1〜15日）に手動実行すると、
まだ進行中の期間を書き出す**。トリガーは16日固定なので自動実行では起きない。
`tools/export_sim.js` にこの挙動を記録してある。

---

## 触るときの注意

- **GAS再デプロイは「デプロイを管理 → 編集 → 新しいバージョン」**。保存だけ／「新しいデプロイ」ではURLが変わって反映されない。反映に数分のラグあり
- **`setupPassword` を再実行すると全端末がログアウトされる**。パスワードを変えるだけならアプリの設定画面から
- **本番の家計データに直結している**。テスト記録を入れたら必ず `deleteLogItem` で消すこと
- ローカル検証は `.claude/launch.json`（port 3456）+ ブラウザツールで `fetch` をモックに差し替える方法が有効。
  **本番GASに絶対に届かせないこと**（`read_network_requests` で `script.google.com` が0件なのを確認する）
- Browserペインの `computer` 系（スクリーンショット含む）はペイン非表示だとタイムアウトする。
  `javascript_tool` でのDOM実測に切り替えると速い
- **GAS を直したら `node tools/auth_sim.js`（44項目）と `node tools/export_sim.js`（16項目）を通すこと。**
  集計の再計算まわりには別途 `recalc_sim.js` を書いたことがある（そちらはscratchpadなので残っていない）

---

## 残っている既知の問題（コードレビュー由来）

| 重要度 | 内容 |
|---|---|
| 中 | 検索語にHTML特殊文字が含まれるとハイライト表示が壊れる（`highlightMatch` のオフセット計算） |
| 中 | `getAllMonthsData` が「最終行＝集計行」決め打ち。シート下部に何か書き足すと全月の集計が0になる |
| 中 | 起動直後のキャッシュ表示と実データの月がずれる可能性（通信完了前に保存すると別月に入る恐れ） |
| 低 | ログイン失敗ロックが全体で1つ（GASから接続元IPが見えないため）。第三者が失敗を繰り返すと家族も15分入れない。`resetLoginLock()` で解除できるので実害は小さいと判断した |
| 低 | `CATEGORIES` がフロントとGASに二重定義、`COLS` はデッドコード。カテゴリ追加時に5箇所の手修正が必要 |
| 低 | 取引ログは論理削除で増え続ける。数年後に月別分割が必要（現在20行程度なので今は不要） |

**次にやるなら `getAllMonthsData` の最終行決め打ち**を推奨。シートを少し触っただけで全月の集計が
0になる作りで、気づきにくい割に影響が大きい。

---

## 性能の現状

- 入力：約4ms（ローカル書き込み）
- 同期：3件1通信で約6.3秒。GASのAPI往復回数が支配的で、これ以上は構造的に削りにくい
- ログイン：ハッシュ2000回ぶんの計算が乗る。本番実測で温まった状態なら823ms、
  冷えた状態なら約1.8秒（computeDigestは1反復あたり温0.41ms/冷0.92ms）。
  90日に1回なので許容範囲。パスワード変更は2回ぶんなのでこの倍
