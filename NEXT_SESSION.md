# 次セッションへの引き継ぎ（2026-08-21）

## 最優先：未コミット・未反映の状態

**作業ツリーが未コミットです。** 最後のコミットは `04be4ff`。以下が未コミット：

```
M css/style.css   M gas/Code.gs   M index.html   M js/app.js   ?? manifest.json
```

反映状況：
- **GAS（Code.gs）→ デプロイ済み**。`syncEntries` まで本番反映・動作確認済み
- **フロント3ファイル + manifest.json → 配信先へ未反映**。最後に直した2バグ（下記）が入っていない

次セッション冒頭でやること：
1. 内容を確認してコミット
2. フロントを配信先へ反映（`?v=20260821b` にキャッシュ更新済み）

---

## このセッションでやったこと

### 修正したバグ
1. **保存の二重加算** … `gasCall` のリトライが成功済みの書き込みを再送していた → `requestId` + `CacheService` で冪等化
2. **個別明細が表示されない** … Sheetsが `"2026年08月"` `"2026/08/21"` を勝手にDate型へ変換し、文字列比較が常に失敗していた → 列を文字列書式に固定 + `normalizeLogDateValue()` で読み出し時に復元
3. **過去月で記録できない** … 入力モーダルの日付初期値が常に「今日」だった → 一覧に無ければ月初にフォールバック
4. **カード種類「その他」の自由入力が消える** … 選択肢に無い名前が「なし」に落ちていた（`renderEditForm` と `renderEditLogItemForm` の両方）

### 入れた仕組み
- **書き込みの直列化** … `LockService`。同時利用で金額が消える問題を排除
- **集計セルの全再計算** … 「残高ベース + 有効ログ」から毎回再計算。差分加算をやめた。隠しシート `残高ベース` がログ導入前の内訳不明分を保持
- **認証** … 全リクエストに合言葉（`setupApiSecret` で発行済み、`app.js:9` に設定済み）。`doGet` は封鎖
- **ローカル先行入力 + 同期ボタン** … 入力は約4msでキューへ。同期はN件を1通信。5日以上未同期なら起動時に警告ダイアログ
- `manifest.json` 追加、`navigator.storage.persist()` 要求

---

## 触るときの注意

- **GAS再デプロイは「デプロイを管理 → 編集 → 新しいバージョン」**。保存だけ／「新しいデプロイ」ではURLが変わって反映されない。反映に数分のラグあり
- **`setupApiSecret` の再実行は不要**（実行すると合言葉が変わり `app.js` の書き換えが必要になる）
- **本番の家計データに直結している**。テスト記録を入れたら必ず `deleteLogItem` で消すこと
- ローカル検証は `.claude/launch.json`（port 3456）+ ブラウザツールでモックを注入する方法が有効
- Browserペインの `computer` 系はタイムアウトしがち。`javascript_tool` でのDOM実測に切り替えると速い
- 不変条件の検証用シミュレーションを書くと安全（前回はscratchpadに `recalc_sim.js` を作成、8シナリオ）

---

## 残っている既知の問題（コードレビュー由来）

| 重要度 | 内容 |
|---|---|
| 重大 | **パスワード認証が実質機能していない**。別端末やシークレットウィンドウでは任意の文字列でログインできる（サーバー検証なし）。合言葉方式と統合すれば本物の鍵にできる |
| 中 | 検索語にHTML特殊文字が含まれるとハイライト表示が壊れる（`highlightMatch` のオフセット計算） |
| 中 | `getAllMonthsData` が「最終行＝集計行」決め打ち。シート下部に何か書き足すと全月の集計が0になる |
| 中 | 起動直後のキャッシュ表示と実データの月がずれる可能性（通信完了前に保存すると別月に入る恐れ） |
| 低 | `CATEGORIES` がフロントとGASに二重定義、`COLS` はデッドコード。カテゴリ追加時に5箇所の手修正が必要 |
| 低 | 取引ログは論理削除で増え続ける。数年後に月別分割が必要（現在20行程度なので今は不要） |

**次にやるならパスワード認証**を推奨。実害があり、他は当面問題にならない。

---

## 性能の現状

- 入力：約4ms（ローカル書き込み）
- 同期：3件1通信で約6.3秒。GASのAPI往復回数が支配的で、これ以上は構造的に削りにくい
- 入力体験と同期の遅さは切り離せたので、同期側の最適化は当面優先度低い
